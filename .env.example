# Copy this file to .env (same folder as docker-compose.yml) and fill in your real key.
# .env is gitignored -- never commit your real API key.

RESCUEGROUPS_API_KEY=

# Optional -- defaults to 3000 if unset.
PORT=3000
